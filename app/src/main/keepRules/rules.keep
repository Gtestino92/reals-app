# Project-specific R8 rules for the Reals Android app.
#
# The current app code does not require app-specific keep rules beyond:
# - AGP default optimized Android rules.
# - Consumer rules from Retrofit, OkHttp, Kotlin Serialization, Firebase, Compose, and AndroidX.
# - AGP-generated rules for manifest-declared Android components and resources.
#
# Add narrow rules here only after a concrete optimized-build or optimized-APK runtime failure
# proves dependency consumer rules and generated Android rules are insufficient.
